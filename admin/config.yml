backend:
  name: git-gateway
  branch: main

media_folder: "images/uploads"
public_folder: "/images/uploads"

publish_mode: simple

collections:
  - name: "site_content"
    label: "Site Content"
    files:
      - label: "Events"
        name: "events"
        file: "data/events.json"
        format: "json"
        fields:
          - label: "Events"
            name: "items"
            widget: "list"
            summary: "{{fields.title}} — {{fields.date}} — {{fields.category}}"
            fields:
              - label: "Title"
                name: "title"
                widget: "string"
                hint: "Example: Let’s Talk, Ward 1 – Meet & Greet"

              - label: "Event Date"
                name: "date"
                widget: "datetime"
                format: "YYYY-MM-DD"
                date_format: "YYYY-MM-DD"
                time_format: false

              - label: "Event Time"
                name: "time"
                widget: "string"
                hint: "Example: 3:00 PM – 4:00 PM"

              - label: "Category"
                name: "category"
                widget: "select"
                options:
                  - { label: "Meet & Greet", value: "meet-greet" }
                  - { label: "Clean Up", value: "cleanup" }
                  - { label: "Canvassing", value: "canvassing" }
                  - { label: "Town Hall", value: "townhall" }
                  - { label: "Fundraiser", value: "fundraiser" }
                  - { label: "Community Event", value: "community" }

              - label: "Address"
                name: "address"
                widget: "string"
                hint: "Full address as it should appear on the website"

              - label: "Description"
                name: "description"
                widget: "text"
                hint: "Short description for the public website"

              - label: "RSVP Link"
                name: "rsvp"
                widget: "string"
                required: false
                hint: "Use full link starting with https://"

              - label: "Event Image"
                name: "image"
                widget: "image"
                required: false

              - label: "Featured Event"
                name: "featured"
                widget: "boolean"
                default: false
                required: false